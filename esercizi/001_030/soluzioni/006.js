// Dichiarare una variabile `lunghezza` e `larghezza`, calcolare l'area di un rettangolo.

// Dichiarazione delle variabili `lunghezza` e `larghezza` e assegnazione dei valori 5 e 10
let lunghezza = 5;
let larghezza = 10;

// Calcolo dell'area del rettangolo
let area = lunghezza * larghezza;

// Stampa dell'area in console
console.log(area);