// Calcolare il resto della divisione tra due numeri.

// Dichiarazione delle variabili `a` e `b` e assegnazione dei valori 10 e 3
let a = 10;
let b = 3;

// Calcolo del resto della divisione di `a` per `b`
let resto = a % b;

// Stampa del resto in console
console.log(resto);