// Creare due variabili `a` e `b`, assegnargli due numeri e stampare la loro somma.

// Dichiarazione delle variabili `a` e `b` e assegnazione dei valori 5 e 10
let a = 5;
let b = 10;

// Calcolo della somma di `a` e `b`
let somma = a + b;

// Stampa della somma in console
console.log(somma);